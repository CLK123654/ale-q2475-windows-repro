# Q2475 Windows reproduction

This public repository contains one Kubernetes manifest authoring task and its Windows Server 2025 workflow. The workflow uses native Windows kubectl and built-in Kustomize without connecting to an API Server.
